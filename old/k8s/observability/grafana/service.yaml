apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: fiduciary-system
  labels:
    app: grafana
spec:
  type: ClusterIP
  ports:
  - name: web
    port: 3000
    targetPort: web
    protocol: TCP
  selector:
    app: grafana
