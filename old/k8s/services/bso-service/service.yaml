apiVersion: v1
kind: Service
metadata:
  name: bso-service
  namespace: fiduciary-system
  labels:
    app: bso-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3002
    targetPort: http
    protocol: TCP
  selector:
    app: bso-service
