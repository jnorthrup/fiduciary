apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: fiduciary-system
  labels:
    app: banking-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3004
    targetPort: http
    protocol: TCP
  selector:
    app: banking-service
