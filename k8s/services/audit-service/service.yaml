apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: fiduciary-system
  labels:
    app: audit-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3006
    targetPort: http
    protocol: TCP
  selector:
    app: audit-service
