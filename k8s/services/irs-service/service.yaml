apiVersion: v1
kind: Service
metadata:
  name: irs-service
  namespace: fiduciary-system
  labels:
    app: irs-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3001
    targetPort: http
    protocol: TCP
  selector:
    app: irs-service
