apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: fiduciary-system
  labels:
    app: ledger-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3003
    targetPort: http
    protocol: TCP
  selector:
    app: ledger-service
